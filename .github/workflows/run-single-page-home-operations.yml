name: Run single page operations patch

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/run-single-page-home-operations.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Apply patch
        run: python3 .github/scripts/single_page_home_operations.py

      - name: Validate
        run: |
          grep -q 'SINGLE-PAGE-HOME-OPERATIONS-V2' index.html
          grep -q 'id="homeOperations"' index.html
          grep -q 'Équilibre 50 / 30 / 20' index.html
          grep -q 'Reste à vivre ce mois-ci' index.html
          grep -q 'function renderOperations()' index.html

      - name: Commit app
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "Place Operations below balance on Home"
          git push origin main

      - name: Deploy Firebase
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_MYBUDGETMANAGERAPPLICATION }}'
          channelId: live
          projectId: mybudgetmanagerapplication
